import { Component } from "@angular/core";
/**
 * Pesquisa permite pesquisar globalmente ou no contexto atual. Unificando a pesquisa 
 * do sistema permitindo filtros específicos para cada formato e tipo de informação 
 * de filtragem e forma de operação para multi filtros.
 */
@Component({
    selector: 'ci-pesqusiar',
    standalone: false,
    template: ``,
})
export class PesquisarComponent {
    constructor() {
    }
}