import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ThrejsComponent } from './threjs.component';

describe('ThrejsComponent', () => {
  // let component: ThrejsComponent;
  // let fixture: ComponentFixture<ThrejsComponent>;

  /* beforeEach(async () => {
    await TestBed.configureTestingModule({
      imports: [ThrejsComponent]
    })
    .compileComponents();

    fixture = TestBed.createComponent(ThrejsComponent);
    component = fixture.componentInstance;
    await fixture.whenStable();
  }); */

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
